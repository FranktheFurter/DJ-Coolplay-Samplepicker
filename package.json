{
  "name": "sample-picker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/cli": "^0.1.1",
    "typescript": "^5.9.2",
    "vite": "^7.2.6"
  }
}
